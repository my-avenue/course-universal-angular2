1. f() และ g() เป็น tail call position เพราะทั้งสองเป็นฟังก์ชันที่ถูกเรียกและคืนค่ากลับในตำแหน่งท้ายสุด
2. f() และ g() ไม่ใช่ tail call position เพราะจะเก็บในตัวแปรก่อนเพื่อใช้ในการตรวจสอบเงื่อนไข
3. 
- f() ไม่เป็น tail call position เพราะไม่ใช่ฟังก์ชันในตำแหน่งท้ายสุด
- g() เป็น tail call position เพราะเป็นฟังก์ชันที่ถูกเรียกและคืนค่ากลับในตำแหน่งท้ายสุด